// import { DateTime } from 'luxon';

// export function getTodayInTimeZone(timeZone: string): string {
//   // Get the current time in the specified time zone
//   return DateTime.now().setZone(timeZone).toISO(); // ISO 8601 string
// }
